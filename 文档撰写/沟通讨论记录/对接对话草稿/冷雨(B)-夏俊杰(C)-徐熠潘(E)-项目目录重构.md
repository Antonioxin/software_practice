# 冷雨（B）、夏俊杰（C）、徐熠潘（E）：项目目录重构

> 草稿性质：根据成员回忆和仓库中的目录重构 PR 整理；不是逐字聊天记录。
>
> 参与成员：冷雨（B：商品与库存）、夏俊杰（C：零售交易）、徐熠潘（E：内容与文件）

## 讨论：项目仍沿用 A 成员目录

**夏俊杰（C）：** 我准备接商品模块开始做交易这部分了，不过现在整个工程是不是还放在 `partA` 下面？

**冷雨（B）：** 对，我当时是在 A 已经写好的项目上直接接着开发，所以 B 的商品和库存也放进去了。

**夏俊杰（C）：** 现在已经不只是 A 的代码了，如果后面 C、D、E、F 都继续往这里放，目录看起来会比较乱。

**徐熠潘（E）：** 我也觉得需要整理。成员字母可以表示分工，但项目代码不应该继续按成员目录来分。

**冷雨（B）：** 可以，我当时主要是为了直接接现有工程，没有再改外层结构。

**徐熠潘（E）：** 那我先把项目代码整理到统一的 `project-implementation` 下面，前后端按整个项目来放。

## 讨论：后端包结构也存在类似问题

**夏俊杰（C）：** 外层目录改完以后，后端里面还有一个问题。现在商品代码是在 `identity` 下面，我感觉商品和身份应该是同一级。

**冷雨（B）：** 这个也是因为我直接在 A 的后端工程里往下加，所以当时就接在现有包下面了。

**徐熠潘（E）：** 既然外层已经统一成项目结构，Java 包这里也最好一起理顺。

**夏俊杰（C）：** 那我来改后端这部分。最外层统一成 `wemove`，下面再放 `identity`、`catalog`、`commerce` 这些模块。

**冷雨（B）：** 可以，只要商品原来的功能不受影响就行。

**夏俊杰（C）：** 我只整理目录和包结构，改完把现有测试再跑一遍。

**徐熠潘（E）：** 行，后面其他模块也按这个结构继续接。

## 讨论结论

1. 徐熠潘将项目代码从按成员划分的目录整理到统一的项目实现目录。
2. 夏俊杰进一步调整后端 Java 包结构，使 `identity`、`catalog`、`commerce` 等业务模块保持平级。
3. 三人同意后续新增模块继续沿用统一工程结构，不再使用 `partA`、`partB` 等成员目录组织业务代码。
