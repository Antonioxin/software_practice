# Shared UI Components

The repository does not contain authored application components yet. The only reusable UI source is the captured WEMOVE site in `material/`. The new Role A frontend will use Vue 3 and Element Plus, with project-specific wrappers authored under `project-implementation/apps/web/src/components/`.

Reference logo markup from `material/首页.html`:

```html
<a href="/" class="header-logo">
  <img src="logo.png" alt="WEMOVE SPORTS">
  <span>WeMove</span>
</a>
```

Reference navigation link:

```html
<a href="/workshop" class="hn-link">玩具品类</a>
```
