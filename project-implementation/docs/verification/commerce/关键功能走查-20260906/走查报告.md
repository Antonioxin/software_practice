# 关键功能界面走查与截图

2026-09-06，约18:21—18:34，使用本地隔离环境进行真实浏览器操作。范围为角色C零售交易主流程及其登录、商品入口。新增29张分屏截图，见[截图画廊](截图画廊.md)。本轮未修改应用源码，发现的问题保留在下表供修复。

## 环境与数据

- 浏览器：Codex内置浏览器，默认视口1265×712；本轮未执行移动端或多浏览器矩阵。
- 前端：当前工作区Vite，localhost:15173；后端：此前验证通过的review JAR，localhost:18087。
- 数据库：MySQL8隔离实例，单独新建 `commerce_walkthrough_20260906` 并执行Flyway，未复用历史自动化商品夹具库。
- 合成用户 `walk-user@example.test`，合成管理员 `walk-admin@example.test`；收件人与地址均为测试数据。用户预置通过本地API完成，交易及管理动作通过页面完成。
- 付款、退款、发货均为课程模拟，不产生真实交易。收尾购物车已清空。

## 走查结果

| 功能 | 实际操作与结果 | 截图编号 |
| --- | --- | --- |
| 商品目录与详情 | 14个公开商品正常展示；进入投环组，价格¥129.00、可售状态正常 | 01、02 |
| 登录拦截与返回 | 未登录点击加车跳转登录；登录后返回原商品，再次点击加车成功 | 03 |
| 加车与改量 | 2件合计¥258.00；键盘修改为1件后合计¥129.00 | 04、05 |
| 结算校验 | 非数字电话被规范化为空，提交得到6—20位电话提示；其他已填地址保留 | 06 |
| 可信金额预览 | 显示商品、数量、合计与0运费/额外税费/优惠；预览15分钟 | 07 |
| 建单 | 创建待付款订单，商品金额与地址成为快照，购物车清空 | 08、18 |
| 模拟付款失败 | 失败后保持待付款，付款尝试和历史保留 | 09 |
| 模拟付款成功 | 从待付款进入待发货，金额保持快照价；失败后可再次成功付款 | 10 |
| 管理员筛选 | 待发货筛选返回目标订单，可进入管理详情 | 11 |
| 模拟发货 | 输入SIM-WALK-20260906并确认，生成物流信息和发货历史；发货后无取消动作 | 12—14 |
| 本人收货 | 用户确认收到整单，订单进入已完成，历史保留完整变化 | 15—17 |
| 付款后取消 | 填写原因并确认取消；进入已取消，生成一笔¥129.00模拟退款 | 19—21 |
| 本人订单列表 | 展示完成与取消订单；筛选已取消只返回取消单 | 22、23 |
| 缺货商品 | 保龄球详情明确缺货，“暂不可购买”按钮禁用 | 24 |
| 未付款取消 | 两件¥258.00订单模拟付款失败后取消，没有生成退款 | 25 |
| 移除与清空 | 分别点击移除、清空购物车，均显示空车状态 | 26、27 |

本轮选定关键操作均完成，未发现阻断上述业务链路的问题。浏览器捕获的error/warn日志为空，见[浏览器日志](浏览器日志.json)。这不代表所有页面、边界或正式验收条目均通过。

## 已发现的界面问题

| 优先级 | 问题与复现 | 影响与建议 | 证据 |
| --- | --- | --- | --- |
| P2 | 结算页“确认快照价并创建模拟订单”文字在深绿色按钮背景上仍呈深色 | 主要操作文字难辨认。明确为主要按钮设置浅色文字，并检查通用按钮规则的覆盖关系 | 28 |
| P3 | 已完成/已取消订单，以及管理员查看已发货订单时，没有可用动作但仍出现空白白色卡片 | 多余空面板占用空间，容易误以为内容未加载。仅在存在可用动作、确认表单或恢复请求时显示操作容器 | 13、29 |

相关源码：[公共交易按钮样式](../../../../apps/web/src/features/commerce/style.css)、[结算按钮](../../../../apps/web/src/pages/commerce/CheckoutPage.vue)、[订单操作面板](../../../../apps/web/src/pages/commerce/OrderDetailPage.vue)。本轮为走查取证，未顺带修改这些实现。

## 数据库交叉核对

| 订单ID | 结果 | 总额（分） |
| --- | --- | --- |
| 086b4834-45bb-4eb3-a4de-7715d216a852 | COMPLETED | 12900 |
| c4b82ae3-fc23-449b-8e83-1c700b8a5cc0 | CANCELLED，已付款取消 | 12900 |
| 5b30c8c9-f77f-4540-8bd0-31526ddd8e94 | CANCELLED，仅失败付款 | 25800 |

退款表共1条；投环组库存从V2种子的24件变成23件，符合完成单扣1、付款取消单返还1、失败付款单不扣库存的净效果。

## 证据说明与边界

截图按功能阶段编号，部分画面在同一流程再次经过时重拍，因此编号不是严格的时间顺序；第三单的失败付款截图金额为¥258.00。截图07展示的是结算金额区域，错误提示在再次提交前仍保留；不能据此宣称错误提示会随输入立即消失。

长页fullPage抓取最初出现重复拼接，已全部换成正常视口分屏截图，不对截图进行拼贴或生成式修改。操作成功依据页面状态/历史及SQL核对，截图用于界面证据。数量直接fill未触发原生change时，改用方向键及Tab确认变更成功，未将工具输入差异判为应用缺陷。

未在本轮重跑Maven/Vitest，未执行并发、断网、越权、移动端或24小时恢复矩阵；这些与上一轮记录分开。26条C及159条全组正式设计仍待审核，本报告不批量改变其状态。应用未提交、推送或部署。

## 后续：角色 C 前端风格统一复查

上文是统一版改造前的历史走查，表中 P2/P3 问题随后已在当前工作区修复。2026-09-06 20:25 后使用当前源码、隔离 MySQL、Codex 内置浏览器重新走查，补充证据集中在[角色 C 统一版截图画廊](角色C统一版截图画廊.md)。

| 复查项 | 实际结果 |
| --- | --- |
| 结算主要按钮与弹窗动作 | 结算、模拟付款、模拟发货、确认收货、取消和确认按钮均使用统一浅色文字/绿色或金色操作色；空地址提交显示全局错误并聚焦首个字段 |
| 终态订单详情 | 已完成、已取消订单只保留金额/状态说明，不再渲染无动作的空白操作卡片；付款失败、成功、退款和历史仍完整展示 |
| 768px / 390px | 订单长号在 768px 堆叠并可换行；390px 导航折叠，购物车、结算、订单详情均按单列呈现 |
| 管理员商品与订单 | 商品列表、编辑页、库存调整弹窗、订单列表状态筛选、完成订单详情均真实打开；保留 B 模块业务语义 |
| 真实交易链 | 第一单完成“付款失败 → 成功 → 管理员发货 → 用户收货”；第二单完成“付款成功 → 用户取消 → 模拟退款” |

本次源码修改与复查的命令结果详见[角色 C 本轮实现与验证记录](../../../modules/commerce/验证记录.md)。新一轮仍未声称 26 条角色 C 或 159 条全组验收设计已逐条通过，也未执行多浏览器、断网/响应丢失和压力矩阵。
