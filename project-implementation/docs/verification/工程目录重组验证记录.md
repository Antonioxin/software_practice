# 工程目录重组验证记录

日期：2026-09-06。检查范围：按软件工程职责重新组织后的本地工作区。

## 组织方式与变更范围

- 共用前端集中到 `apps/web/`，后端集中到 `apps/api/`；业务源码、单元测试和 Flyway 迁移仍采用各自工程的既有结构。
- HTTP 契约集中到 `contracts/openapi/`，身份与商品库存冒烟脚本集中到 `scripts/smoke/`。
- 成员和集成测试设计集中到 `tests/cases/`；模块资料按 `docs/modules/identity/`、`docs/modules/catalog/` 归档，截图按设计或验证用途归档。
- 环境模板、本机配置、设计工具资料和本地验证资料位于工程根目录。原需求、课程参考、进度图与视觉参考的位置保持不变。
- 前端工程名改为 `wemove-web`，后端构建名称改为 `wemove-api`；身份冒烟脚本据自身位置寻找工程根目录及新 JAR。Java 包名、对外 API、业务逻辑和既有数据库迁移内容保持不变。

以上路径均相对于 `project-implementation/`。详细入口见 [工程说明](../../README.md)。

## 本次已执行检查

以下命令均从仓库根目录运行；后端使用本机已缓存的 Maven 依赖。

| 检查 | 方式或命令 | 结果 |
| --- | --- | --- |
| 前端类型检查及生产构建 | `npm --prefix project-implementation/apps/web run build` | 通过 |
| 现有前端单元测试 | `npm --prefix project-implementation/apps/web run test` | 2 个测试文件、4 项测试通过 |
| 后端编译、现有单元测试及打包 | `mvn -o -f project-implementation/apps/api/pom.xml verify` | 9 项测试通过，生成 `wemove-api-1.0.0.jar` |
| 文件保留情况 | 迁移前后文件清单及符号链接比对 | 无遗漏，符号链接目标未变 |
| 业务与契约内容 | 对 110 份源码、迁移、契约、配置及图片计算 SHA-256 并比对 | 内容一致 |
| 工程名称与启动产物 | 核对 npm 清单、锁文件、Maven 产物与冒烟脚本引用 | 一致 |
| 脚本语法 | `bash -n project-implementation/scripts/smoke/identity-api.sh` | 通过 |
| 忽略规则 | 检查前后端产物、依赖、本机 `.env` 和 `.local/` | 在新路径继续忽略 |
| 文档与工具路径 | 检查自有 Markdown 相对链接、设计工具元数据及残留旧入口 | 已同步至新结构 |
| 文本空白 | `git diff --check` | 通过 |

## 验证边界

本次未运行数据库或 API 冒烟，未重新执行浏览器业务验收。身份脚本的语法检查不代表完整接口链路通过；实际运行仍需 Linux／WSL 及专用测试数据库。商品脚本需要 PowerShell 和已启动的测试应用。

此前设计的 **159 条成员及集成测试用例仍待用户审核、未执行**；本轮通过的是工程现有的 **13 项单元测试**。各模块原有验证记录和成员 B 审核中的发现、结论仍独立有效，目录重组不代表这些问题已修复。
