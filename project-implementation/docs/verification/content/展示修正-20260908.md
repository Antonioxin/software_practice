# 灵感封面与商品名称展示修正

2026-09-08，按用户反馈修正文章封面裁切与商品文案。

- 文章封面显式使用可收缩的 Grid 行列，图片设置 `min-height: 0`、`min-width: 0`，继续按 `object-fit: contain` 完整展示。悬停改为轻微上移。
- 商品主数据通过 V13 去除名称末尾“ · 示例”或“示例”，同步递增版本；订单和询价历史快照只清理显示名称。开发预览商品名同步更新。
- 探索商品卡片下方不再显示图片说明文字。

## 实际验证

- 浏览器实测修复前封面高 245 px、图片高约 381 px；修复后两者均为 245 px，人物与全部平衡石完整可见。
- `ProductsPage.spec.ts` 13 项通过；商品名相关的 catalog presentation、OrdersPage、预览 scenes 测试共 42 项通过。
- 前端 TypeScript 与 Vite 构建通过。后端使用 `mvn -DskipTests clean package` 成功打包；本次未重跑后端测试。`clean` 清除了同步产生的重复构建文件。
- 原 MySQL 成功执行 V13，商品 API 返回“平衡石、彩虹拱桥、环环投掷、伙伴协作板、森林探索套装、轻盈跳绳”。浏览器确认相同名称，并确认商品卡片图片说明节点为 0。
- `git diff --check` 通过。

本地服务继续使用前端 5173、后端 8080。当前运行 JAR 为忽略目录中的 `.local/content-runtime/wemove-display-api.jar`。升级前商品备份为 `.local/database-backups/products-before-name-cleanup-20260908.sql`，不加入版本库。
