# 经销合作验证记录

验证时间：2026-09-07（Asia/Shanghai）。本记录只登记本轮实际自动化结果，不把未执行的成员验收用例标记为通过。

| 检查 | 结果 | 覆盖范围 |
| --- | --- | --- |
| `mvn -f apps/api/pom.xml -DskipTests compile` | 通过 | 99 个后端源文件编译、D 实体/控制器/服务与 A/B/F 端口装配 |
| `mvn -f apps/api/pom.xml -Dtest=DealershipWorkflowIntegrationTest test` | 通过，1/1 | 注册登录、申请、管理员审核、资格即时生效、经销目录、超库存询价、管理员回复、公开渠道显式发布 |
| `mvn -f apps/api/pom.xml test` | 通过：38 个，17 个执行通过、21 个既有 MySQL 条件测试跳过 | 原 A/B/C 回归、应用扫描、D 规则、D 闭环集成测试与 OpenAPI 解析校验 |
| `mvn -f apps/api/pom.xml -Dtest=DealershipOpenApiTest test` | 通过，1/1 | 使用 SnakeYAML 解析 `contracts/openapi/dealership.yaml`，校验根结构、26 条路径及全部本地引用 |
| `npm run test -- --run` | 通过，3 个文件共 9 项 | 现有前端单元回归 |
| `npm run build` | 通过 | Vue TypeScript 检查与生产构建，包含全部 D 页面路由 |

未执行：真实 MySQL V8 迁移、成员 D 的 26 条详细测试、三屏宽浏览器截图、并发审批/故障注入、E 的经销资料文件联动以及 F 的总览页面。对应设计状态继续以[成员 D 测试用例](../../../tests/cases/成员D测试用例.md)为准。

OpenAPI 文件同时完成了人工检查和自动解析校验。
