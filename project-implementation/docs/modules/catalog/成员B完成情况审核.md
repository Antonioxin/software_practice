# 成员 B 完成情况审核

目录整理说明（2026-09-06）：实现、文档和本机证据随模块移入 `project-implementation/`；本报告仅更新当前路径，原审核提交、发现和结论保持不变。

审核日期：2026-09-06。审核提交：`0157ab2`（PR #3），比较基线：`b83e7b5`。范围为 B 的三个提交 `6b45ce6`、`cc664c1`、`3f19fc4`，共 63 个文件，主要业务代码接入 `project-implementation` 的共同应用。

**结论：可以认定 B 已交付可运行的商品与库存初版，但暂不应标记为完整验收通过，建议状态为“修改后复验”。** 正常商品管理链路已经跑通；库存取消返还、失败重试和接口边界仍有明确缺陷。与 C、D、E 的联合功能另行登记为待集成。

这里的 P1/P2 是缺陷修复优先级，不是 SRS 中的需求优先级：P1 应优先修复，避免错误业务数据；P2 应在本模块验收前修复。判断依据为 SRS、接口契约及团队协作规范中的完成标准，不以代码量或截图数量计算完成百分比。

## 1. 已确认的缺陷

### R1 · P1：取消返还相信调用方数量，没有按原扣减记录恢复

- 位置：[InventoryService.java](../../../apps/api/src/main/java/com/wemove/identity/catalog/service/InventoryService.java)，第 118—130 行，实际增量取值在第 128 行。
- 复现：商品库存为 10，同一订单先调用 `deductForPayment(orderId, [(productId, 1)])`，再调用 `restoreForCancellation(orderId, [(productId, 99)])`。
- **真实 MySQL 结果：库存依次为 10 → 9 → 108；扣减流水数量 1，返还流水数量 99。** 正确结果应为恢复至 10，或者拒绝与原扣减明细不符的返还请求。
- 原因：只通过 `existsByProductIdAndSourceTypeAndSourceId` 确认存在扣减，未读取原扣减数量，直接将 `item.quantity()` 加回。
- 依据：接口契约 7.1 要求“只按该订单原扣减记录返还一次”；SRS BR-02 要求增加此前扣减数量。
- 修复方向：从该订单的原扣减流水构造返还明细并校验完整性，保留业务唯一约束和事务，不能仅检查流水存在。

此问题在供 C 调用的内部服务接口中，当前公开网页没有直接调用入口；不能据此声称已有真实订单出错。但该接口尚不具备所承诺的接入保证，应在 C 接入前修复。

### R2 · P1：库存提交结果未知时，重试生成新键，可能重复调整

- 位置：[AdminProductsPage.vue](../../../apps/web/src/pages/AdminProductsPage.vue)，第 69—81 行，关键为第 74 行。
- 触发：后端已提交“增加 2 件”，响应在途中丢失；页面显示“库存调整失败”，保留原弹窗。用户再次点击确认，`adjustStock()` 再次生成新的 `Idempotency-Key`。
- 影响：后端会把新键识别为新的业务操作，再增加 2 件，无法保证同次操作只生效一次。
- 依据：接口契约 6.2 要求同一确认请求重试保留同一键；SRS BR-06 要求区分确定失败和结果未知。
- 修复方向：为同一次未确认的提交保存键和请求内容；响应未知时查询／重试原操作，修改内容并重新确认后才生成新键。
- 证据级别：前后端完整代码路径确认；本次未做网络响应丢失注入。不要将此项表述为已完成真实浏览器断网复现。

“幂等”是同一业务操作重复请求仍只产生一次效果。库存应满足 \(S_{final}=S_{initial}+\Delta\)，而不是因重试两次成为 \(S_{initial}+2\Delta\)。

### R3 · P2：小数金额、年龄和库存被静默截断后保存

- 位置：[CatalogDtos.java](../../../apps/api/src/main/java/com/wemove/identity/catalog/api/CatalogDtos.java)，第 44—49、71—74 行；库存表单确认逻辑见 `AdminProductsPage.vue` 第 132—135 行。
- **真实 HTTP 复现：**创建草稿提交 `{"ageMin":3.9,"retailUnitPriceFen":100.9,"initialStock":1.5}`，返回 201，保存为年龄 3、价格 100 分、库存 1。库存调整提交 `quantity:1.5` 返回 200，库存从 10 变为 11。
- 原因：整数 DTO 在当前 Jackson 配置下接受小数并截断，业务校验看到的已经是整数。库存确认按钮位于表单外并直接调用提交函数，也没有整数检查，不能依赖浏览器表单步长限制。
- 修复方向：服务端禁止小数强制转整数，明确返回字段错误；前端库存输入使用 `Number.isInteger` 校验。
- 依据：SRS 3.4 的字段类型、数量与金额单位要求，以及 FR-27 的正整数库存增减。

### R4 · P2：同一幂等键换商品仍执行成功

- 位置：[InventoryService.java](../../../apps/api/src/main/java/com/wemove/identity/catalog/service/InventoryService.java)，第 50—53 行；上下架同类问题见 [CatalogService.java](../../../apps/api/src/main/java/com/wemove/identity/catalog/service/CatalogService.java) 第 182—185 行。
- **真实 HTTP 复现：**同管理员使用同一个 UUID 和相同增加 2 件的请求体，先对商品 A 操作，再对商品 B 操作，两次均返回 200，B 库存也增加 2。
- 原因：资源 ID 被拼进 `operationId`，两个商品成为两个独立去重空间。
- 期望：第二次返回 `409 IDEMPOTENCY_CONFLICT`。接口契约 6.2 明确 operationId 只表示操作模板，资源 ID 应进入请求摘要。
- 修复方向：统一操作模板标识，将目标商品 ID 纳入摘要；库存与上下架同时修正。

### R5 · P2：并发同键请求返回唯一性冲突，而不是成功重放

- 位置：[InventoryService.java](../../../apps/api/src/main/java/com/wemove/identity/catalog/service/InventoryService.java)，第 52—64 行。
- **真实 HTTP 复现：**8 个并发请求，对同商品、同键、同请求体增加 2 件；1 个返回 200，7 个返回 `409 UNIQUE_CONFLICT`。库存总共只增加 2 件。
- 原因：各请求在取商品锁之前检查幂等记录，等待者获得锁后没有重新协调幂等结果，直到写入唯一约束时才失败回滚。
- 修复方向：并发同键请求等待完成后重放已成功结果；超过等待预算可返回契约规定的 `REQUEST_IN_PROGRESS`，不能报告为新操作的唯一性失败。
- **边界：数据库事务和唯一约束阻止了重复库存效果，这一场景没有发生多次加库存。** 问题是错误响应与重试语义，不能误报为已超卖。

### R6 · P2：分类更新响应携带旧版本

- 位置：[CatalogService.java](../../../apps/api/src/main/java/com/wemove/identity/catalog/service/CatalogService.java)，第 240—244 行。
- **真实 HTTP 复现：**创建分类版本 0；PATCH 修改成功后响应仍为 0；重新 GET 已为 1。直接使用 PATCH 返回的版本继续修改，返回 409。
- 原因：在 JPA 将版本递增写入数据库之前，已构造不可变响应 DTO。
- 修复方向：构造响应前 flush，返回修改后的版本；增加连续两次更新的接口测试。
- 现有前端保存后会重新 GET，所以当前页面部分掩盖了接口缺陷；接口调用方不能可靠使用更新响应继续操作。

### R7 · P2：输入 0 岁后年龄条件被丢弃

- 位置：[ProductsPage.vue](../../../apps/web/src/pages/ProductsPage.vue)，第 54—57 行；数字输入在第 89 行。
- 原因：Vue 数字输入将 `0` 转为数值，`if (value)` 把它当作空条件，没有写入 URL。
- 证据：使用仓库现有 Vue 与 jsdom 最小挂载复现，输入 0 得到 `model=0, query={}`，输入 6 则保留年龄条件。真实接口 `age=0` 能正确返回空结果，问题在前端条件收集。
- 修复方向：区分空字符串、null、undefined 与数值 0，按字段构建查询并补边界测试。
- 依据：FR-04/TC-03；年龄为合法的 0—18 整数，筛选规则为 \(age_{min}\leq a\leq age_{max}\)。

### R8 · P2：库存流水界面只能查看最新 20 条

- 位置：[AdminProductsPage.vue](../../../apps/web/src/pages/AdminProductsPage.vue)，第 84—90、138—141 行。
- 原因：固定请求 `page=1&pageSize=20`，丢弃分页元数据，弹窗没有翻页或加载更多入口。
- 影响：同一商品超过 20 条流水后，管理员无法从页面检查更早的初始入库、调整或交易记录，影响 FR-27 的余额与流水核对。
- 修复方向：使用后端已有分页能力，显示总数、当前页及翻页／加载更多入口。
- 证据级别：代码确认；本次没有新增 21 条记录做浏览器操作复现。

### R9 · P2：非法查询类型被当成服务内部错误

- 位置：[PublicCatalogController.java](../../../apps/api/src/main/java/com/wemove/identity/catalog/api/PublicCatalogController.java)，第 20—26 行；需与 A 的统一异常处理协同修复。
- **真实 HTTP 复现：**`/products?page=abc`、`age=3.5`、`categoryId=bad` 均返回 `500 INTERNAL_ERROR`，提示服务暂时不可用。
- 期望：接口契约 2.2 规定非法查询类型或范围返回 422 并指出问题字段。
- 修复方向：为请求参数类型转换异常补充统一映射；有效参数校验已在控制器内，转换失败发生在进入控制器之前。

### R10 · P2：特殊关键词被解释为通配符

- 位置：[CatalogService.java](../../../apps/api/src/main/java/com/wemove/identity/catalog/service/CatalogService.java)，第 55—56 行；后台查询第 118—119 行同样存在。
- **真实 HTTP 复现：**搜索 `%` 或 `_` 返回全部已发布商品，而测试数据的名称和 SKU 不包含这两个字符。
- 原因：用户关键词直接拼入 SQL LIKE 模式，没有将 `%`、`_` 等作为字面字符转义。
- 修复方向：转义 LIKE 通配字符并明确 escape 字符，保留既有参数绑定。
- 这是关键词语义错误；现有查询采用参数绑定，本次没有发现由此产生 SQL 注入。

## 2. C 接入前还需补齐的保证

[CatalogIntegrationService.java](../../../apps/api/src/main/java/com/wemove/identity/catalog/service/CatalogIntegrationService.java) 第 32—46 行的 `getRetailSnapshot` 使用普通 `findById`，未锁定商品，也没有在写订单时核验版本的配套机制。`@Transactional(readOnly=true)` 只是事务设置，不会自动阻止其他事务修改价格或上下架状态。

接口契约 7.1、7.3 要求快照在受保护读取边界内核验，价变与结算创建保护同一商品记录。当前实现不能单独提供这一保证。B 应与 C 选择有序行锁或版本校验方案，并完成“读取商品后另一事务改价／下架”的联合测试。

本项依据代码与契约确认；C 的订单流程尚未接入，本次未生成订单，也不声称已复现实际错单。

## 3. 完成情况矩阵

| 范围 | 当前判断 | 证据与待办 |
| --- | --- | --- |
| FR-03 商品列表、分页、稳定排序 | 主要功能已完成 | 真实 API 验证默认 12 条、第二页无重复、价格升序正确；种子已发布商品 14 个 |
| FR-04 搜索与组合筛选 | 已实现，待修复后验收 | 年龄、场景服务端逻辑存在；R7、R9、R10 待修复 |
| FR-05 详情与购买入口 | B 展示部分已完成，联合验收待完成 | 草稿 404、下架旧链接、缺货不可购、公开响应无经销价已验证；真实加购待 C，媒体和资料待 E |
| FR-26 商品草稿、编辑、上下架 | 主流程已完成，待修复边界 | 实测创建草稿 → 发布 → 编辑已发布商品 → 下架 → 旧链接不可购 → 重新上架全部成功；R3、R4 待修复 |
| FR-27 分类、价格、库存 | 已实现，尚未验收通过 | 引用分类停用拒绝、负库存拒绝、普通商品编辑不能覆盖库存；R1—R6、R8 待修复 |
| BR-02 交易库存保证 | 部分通过，C 接入前仍有阻塞 | 正常返还一次和整单失败回滚通过；原数量返还缺陷已复现；订单级联合并发仍需执行 |
| NFR-03 兼容与可用性 | 部分证据，待完整验收 | B 提交 Chromium 1440/390 截图；第二浏览器、360/768 与键盘验收仍待登记，本次未重新运行真实浏览器 |
| 设计、接口、运行与验收文档 | 已提交模块资料 | `project-implementation/docs/modules/catalog`、OpenAPI、PowerShell 冒烟脚本、10 张截图均存在 |
| B 的课程与公共交付 | 仓库证据待补 | 未见 B 独立 Web 技术现状报告、全组数据字典／ER 图汇总；模块设计中的四表概述不等于全组数据设计交付。只说明当前仓库未见，不推断其是否在仓库外完成 |

团队分工将 FR-06/07 分配给 E、FR-08 分配给 D。它们的占位导航，以及 B 已声明等待 C/D/E 的功能，不应全部算作 B 独立未完成；也不能将占位页计为全站验收完成。

## 4. 本次实际执行的验证

| 检查 | 结果 |
| --- | --- |
| `mvn -B -ntp verify` | 9 个后端测试通过，编译和打包成功；其中 B 新增 5 个实体／规则测试 |
| `npm test` | 4 个前端测试通过；其中 B 新增 2 个展示／查询辅助函数测试 |
| `npm run build` | 类型检查和 Vite 生产构建通过 |
| 全新独立 MySQL schema | MySQL 8.0.45 上 V1/V2 迁移成功，Hibernate 校验成功 |
| HTTP 正常路径 | 商品分页、价格排序、公开字段隔离、草稿/下架/缺货状态、完整发布流程、分类引用保护、负库存拒绝、顺序同键重放通过 |
| HTTP 边界与并发 | R3、R4、R5、R6、R9、R10 已复现 |
| 真实 `InventoryPort` + MySQL | 原库存 10 扣 1 返 99 后为 108；正常扣 3 返 3 并重复返还后为 10、返还流水 1 条；A 库存 5/B 库存 0 整单各扣 1 失败后仍为 5/0、付款流水 0 条 |
| Vue/jsdom 最小复现 | 输入年龄 0 被原查询收集逻辑丢弃 |
| `git diff --check b83e7b5..0157ab2` | 通过 |

审核临时后端已停止，数据库保留本次复现数据。审核使用本机项目 MySQL 中新建的独立数据库 `wemove_review_b_8c2e81574a`，审核 API 端口为 18080，内部服务验证临时端口为 18081。没有对原 `wemove` 业务库执行审核写入。HTTP 初次统计曾包含内部库存验证新增的 4 个已发布测试商品，因此为 18；按 `WM-` 种子标识复核为 14，不能把 18 写成原始种子数量。

本机原始证据位于被 Git 忽略的 `project-implementation/.local/review-b/`：`http-results.json`、`supplemental-results.json`、`InventoryDatabaseProbe.java`、`InventoryDatabaseProbe.log`、`backend.log`。该目录含本机配置，不能整目录提交或分享；本报告已摘录复现输入及关键结果。

## 5. 验收证据应如何补齐

B 提交的截图说明明确写着没有提交新增分类、商品或库存调整；截图证明页面呈现，不能单独证明写入成功。原有 API 冒烟主要覆盖顺序正常路径，现有单元测试主要验证实体或辅助函数，也没有覆盖本次发现的服务级边界。

建议按以下顺序完成复验：

1. 修复 R1、R2，并提交原数量返还、响应未知后同键重试的回归证据。
2. 修复 R3—R10，补数值输入、并发同键、跨商品键冲突、连续分类更新及筛选边界测试。
3. B/C 联合验证锁与订单事务、价格变动、付款竞争、取消返还；B/D/E 分别完成经销目录与媒体引用集成。
4. 补第二浏览器、目标宽度、键盘验证及课程交付文件，再更新 B 的验收追踪和全组追踪表。

主要正常链路已有实测证据，当前需要集中修复业务边界并补联合验收，不需要重新搭建一套应用。
