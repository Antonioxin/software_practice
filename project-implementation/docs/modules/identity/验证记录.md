# 角色 A 验证记录

验证日期：2026-09-05（Asia/Shanghai）

目录整理说明（2026-09-06）：下文路径已更新为当前的 `project-implementation/`，用于定位与复现；原验证日期、环境和结果保持原记录，不代表迁移后的重新验收。

## 环境

| 工具 | 实际版本 |
|---|---|
| Java | OpenJDK 21.0.12 |
| Maven | 3.6.3 |
| Node.js | 24.13.0 |
| npm | 11.6.2 |
| MySQL | 8.0.46，`127.0.0.1:3306` 已监听 |

## 已执行结果

| 检查 | 命令 | 结果 |
|---|---|---|
| 后端编译、单元测试、打包 | `cd project-implementation/apps/api && mvn -q verify` | 通过；4 tests，0 failures，0 errors |
| MySQL/Flyway/API 全链路 | 隔离的本机 MySQL 8.0.46 临时实例 + `project-implementation/scripts/smoke/identity-api.sh` | 通过；迁移、注册、登录、资料、停用、会话撤销、恢复 |
| 前端组件测试 | `cd project-implementation/apps/web && npm test -- --run` | 通过；2 tests |
| 前端类型检查与生产构建 | `cd project-implementation/apps/web && npm run build` | 通过；无大包告警 |
| OpenAPI 基础解析 | PyYAML 读取并检查版本/路径 | 通过；OpenAPI 3.1.0，11 paths |
| 文档相对链接 | 扫描 `project-implementation` 自有 Markdown | 通过；无断链 |
| 品牌素材一致性 | SHA-256 对比原素材与公共资源 | 通过；摘要一致 |
| 文本空白检查 | 行尾空白扫描 | 通过 |

后端测试明细：

- `UserEntityTest`：2 项，验证新账户状态和状态迁移。
- `InMemoryRateLimitServiceTest`：2 项，验证 5 次登录失败封禁与注册窗口限制。

## 运行验证说明

- 为避免猜测或写入用户常驻 MySQL 的 root 密码，本次数据库验证使用 `/tmp` 下隔离的 MySQL 8.0.46 实例和临时端口 3307；未使用 Docker，测试完成后实例及数据目录已清理。用户的常驻 `127.0.0.1:3306` 数据没有被修改。
- 浏览器截图与控制台回归：Vite 本身在 124 ms 内启动成功，但当前环境没有 Chromium、Playwright Python 包或 `agent-browser` 可执行文件；未额外污染项目依赖，保留为具备浏览器环境后的手工/自动回归项。

完整复现步骤和验收路径见《运行与验证手册》。CI 工作流已配置同一套编译和测试命令；浏览器验收仍应在候选交付环境复核。

## 目录整理后的检查（2026-09-06）

本次将 A、B 完整目录移入 `project-implementation/`，保留共用应用和内部结构。以下检查针对目录与引用调整后的本地工作区；上文历史结果保持原样。

| 检查 | 执行位置／方式 | 结果 |
| --- | --- | --- |
| 前端类型检查与生产构建 | `project-implementation/apps/web`：`npm run build` | 通过 |
| 现有前端单元测试 | 同目录：`npm run test` | 4 项通过 |
| 后端编译、现有单元测试与打包 | `project-implementation/apps/api`：`mvn -o verify`，使用本机已缓存依赖 | 9 项通过；构建成功 |
| 移动完整性与代码内容 | 迁移前后目录清单与源码／接口摘要核对 | 原目录项无遗漏；业务源码和接口定义内容未变 |
| 文档与路径 | 检查 Markdown 相对链接、设计元数据路径、脚本语法与 Git 忽略规则 | 路径已同步；构建产物和本机验证资料继续忽略 |

未启动数据库业务流程，也未执行 MySQL/API 冒烟或待审核的 159 个成员及集成用例；本节不改变这些用例的审核和执行状态。
