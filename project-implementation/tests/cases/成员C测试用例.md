# 成员 C 测试用例审核稿

| 文档属性 | 内容 |
| --- | --- |
| 状态 | **设计待用户审核；未执行** |
| 编写日期 | 2026-09-06 |
| 负责人 | 成员 C，姓名尚未确定 |
| 主责范围 | FR-13—FR-18；购物车、结算、模拟付款、本人订单、取消和履约；负责金额/状态机与跨模块交易一致性。 |
| 协作与复核 | A：身份/事务/幂等；B：商品/库存；E：页面壳；F：审计/验收。独立复核按责任表由 A/D/E/F 分场景承担。 |
| 本篇编号 | T-C-01 起；独立细化编号，不替换 SRS 原 TC-01—TC-41 |

## 1. 依据、约定与现状

- 业务验收依据：[SRS V1.1](../../project-requirements/WEMOVE网站重构需求文档.md)，需求基线已评审通过；本文简称“SRS”，原 TC 指其附录 A。
- 职责依据：[团队分工与协作规范](../../project-requirements/WEMOVE团队分工与协作规范.md)第 2 节和[需求责任与验收追踪表](../../project-requirements/WEMOVE需求责任与验收追踪表.md)第 2—4 节。协作规范与追踪安排仍为建议稿。
- 接口设计依据：[接口与数据协作契约 V1.0](../../project-requirements/WEMOVE接口与数据协作契约.md)，本文简称“契约”；目前是**待团队评审的设计建议**，并非已部署接口。路径、字段、HTTP 状态/错误码、版本号、整数分、24 小时的扩展去重范围等契约细节均按建议设计列出，评审调整时同步更新本稿；SRS 的业务规则不随意放宽。
- 仓库已有 A/B 集成应用的 `project-implementation` 源码与运行入口；本轮未启动应用。C/D 完整业务实现及全组真实集成测试环境尚未就绪，最终 OpenAPI、业务测试脚本和故障注入方式需实施时核实。以下是可转成脚本与手工记录的测试设计，**所有用例未执行、未判定通过**。Mock 可用于页面预演，不能替代真实数据库、授权、并发和事务的最终证据。
- 用例中的路径均省略建议前缀 `/api/v1`；写请求正常携带当前会话、有效 CSRF 令牌；标识键 K 为新的合法 UUID，资源修改携带最近读出的版本。测试身份均为合成成人测试账户，邮箱使用 `example.test`。
- “高/中/低”仅表示测试风险和执行顺序，不是 SRS 的 P0/P1/P2 需求等级。数据驱动的每一组输入分别执行并复位；未另说明时各用例使用独立测试库/数据命名空间，不依赖上一用例遗留状态。
- 非法请求一律核对受影响记录数、版本、状态、金额/库存/关系均无非预期变化；不能仅以页面报错作为通过判据。禁止在真实业务库注入故障。所需业务状态通过初始化脚本与正常业务动作构造，不手改数据库制造验收结果。
- 并发步骤使用两个独立会话/连接，在测试屏障同时释放请求；记录请求起止、提交顺序和每个响应。每种竞争用独立初态重复 10 轮是本稿建议的执行安排，不替代锁/事务实现检查，也不声称由 10 轮证明所有调度。
- 执行证据另行登记提交号、环境、时间、执行/复核人、预期与实际、通过状态、缺陷编号及脱敏证据路径；本稿的“证据”栏只规定未来要采集什么，不含实际结果。

- 时间推进约定：除专门验证会话失效的用例外，推进测试时钟超过会话有效期后，以同一测试账号重新登录并取得当前 CSRF，再执行目标操作；同次业务重试保留原幂等键、原请求体和原业务版本，不把会话过期误判为去重失败。登录限次等测试使用其专用时钟与前置状态。

## 2. 本篇基础数据

| 数据代号 | 确定内容 |
| --- | --- |
| 身份 | U1=`c.user1@example.test`、U2=`c.user2@example.test` 为启用普通用户；D1=`c.dealer@example.test` 为有效经销用户；ADMIN=`c.admin@example.test`；DISABLED=`c.disabled@example.test`。密码由测试初始化器生成，不写入证据。 |
| P1/P2 | P1：SKU=`TEST-C-001`、名称“测试商品一”、零售价 2990 分、经销参考价 2000 分、库存 100；P2：SKU=`TEST-C-002`、零售价 5900 分、库存 100；均为已上架。各例可显式覆盖库存/价格。 |
| 额外商品 | P03—P21 为可售测试商品，初始库存 100，用于 20/21 行边界。最大金额子例把 20 个商品单价设为 99999999 分，不改正式业务数据。 |
| ADDR | recipient=“测试收件人”，phone=“+86 (138) 0000-0000”（规范化为 +8613800000000），countryOrRegion=“中国”，region=“上海市”，city=“上海市”，addressLine=“测试路 100 号（测试资料）”。 |
| 订单状态 | 待付=PENDING_PAYMENT；已付/待发货=PAID；已发货=SHIPPED；已完成=COMPLETED；已取消=CANCELLED。O1/O2 是每例通过业务创建所得 ID，不是硬编码生产 ID。 |
| 测量口径 | 金额以整数分核对：2990×3+5900=14870；库存以“初值+调整+取消返还−成功付款扣减”核对。读库仅用来检查持久化事实，不绕过业务动作制造状态。 |

## 3. 详细用例

### T-C-01 购物车合并、改量、移除与持久化

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-13、NFR-01；原 TC-13、TC-40；契约 §5.4、§6.3 |
| 优先级 | 中 |
| 方式/依赖 | 接口脚本＋页面；依赖 A 身份、B 商品与独立数据库。 |
| 前置条件与具体数据 | U1 空车，P1 库存 100、2990 分；U2 空车。 |
| 操作步骤 | 1. U1 用不同 K 依次加 P1×2、P1×3，读取购物车。<br>2. 用当前 cartVersion 设置数量为 4，退出重登、重启测试后端并读取。<br>3. 删除 P1，再添加 P1/P2 后清空；每次读取 U2 购物车与库存。 |
| 预期结果 | 重复加购后只有一行、数量 5、小计 14950 分；修改为 4 后小计 11960 分，重登/重启仍为 4。删除和清空各正确移除目标项，版本按有效修改递增；U2 始终空车。加购、修改、删除均不扣库存。 |
| 证据 | 各步请求响应、重登/重启前后购物车与库存读数、最终行数。 |

### T-C-02 零售数量及整车行数边界

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-13、§3.4.2；原 TC-13；契约 §5.4、§9.2 |
| 优先级 | 高 |
| 方式/依赖 | 参数化接口脚本；依赖 B 提供 21 个可售商品。 |
| 前置条件与具体数据 | 每轮空车；P1 与 P03—P21 库存 100；P2 单独子例库存 3。 |
| 操作步骤 | 1. 分别添加/修改数量为 1、99、0、-1、1.5、100、字符串“2”。<br>2. P1 已有 98 时再加 1，随后再加 1。<br>3. 对库存 3 的 P2 分别填 3、4。<br>4. 依次加 20 个不同商品，再加第 21 个；已有 20 行时增加某一原商品合法数量。 |
| 预期结果 | 整数 1/99 与库存上限 3 允许；0/负数/小数/100/错误类型及超库存拒绝。累加达到 99 允许，达到 100 拒绝且仍 99；前 20 行允许，第 21 行拒绝，已有行合法累加允许。失败不改数量/版本或新增行，库存始终不变；契约错误为 422 或对应库存冲突。 |
| 证据 | 每组输入、字段错误、行数/数量/版本和库存前后对照。 |

### T-C-03 购物车与交易接口的身份、归属及防篡改

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-13—18、FR-11、NFR-02、§3.4.2；原 TC-11、TC-24；契约 §2.3、§3.2—3.3、§5.4 |
| 优先级 | 高 |
| 方式/依赖 | 授权矩阵接口脚本；依赖 A 与测试会话。 |
| 前置条件与具体数据 | U1/U2 各有不同购物车及待付款/已发货订单；ADMIN 管理员；DISABLED 停用账户。 |
| 操作步骤 | 1. 游客、DISABLED 请求受保护购物车/订单接口。<br>2. U2 请求 U1 订单详情、付款、取消、收货；以伪造 userId/ownerId 指定 U1 购物车。<br>3. U1 访问后台订单/发货接口；ADMIN 调个人加购和结算。<br>4. 创建订单请求增加 userId、role、status、unitPriceFen 等未定义字段。 |
| 预期结果 | 未登录拒绝、停用阻断；他人私人资源按契约返回与不存在一致的 404，不含姓名、地址、金额或实际 owner。普通用户后台与管理员个人购物被拒绝；伪造字段按建议契约 422，不能改变身份、归属、状态或价格。所有失败无业务写入；合法经销账户仍能作为普通用户按零售价购买。 |
| 证据 | 角色×动作响应矩阵、响应敏感字段检查、订单/购物车记录与归属对照。 |

### T-C-04 加购后的改价、下架、缺货与整车阻断

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-13—14、BR-01—02；原 TC-14；契约 §5.4、§6.1 |
| 优先级 | 高 |
| 方式/依赖 | 接口脚本＋页面；依赖 B 管理动作。 |
| 前置条件与具体数据 | U1 购物车 P1×2、P2×1；P1 2990 分、库存 100，P2 5900 分、库存 100。 |
| 操作步骤 | 1. 生成预览后把 P1 改为 3990 分，读取购物车并提交旧预览。<br>2. 按更新明细重新确认后提交。<br>3. 独立复位，再分别把 P2 下架或库存调为 0，读取购物车并尝试预览/提交；移除 P2 后再预览。 |
| 预期结果 | 购物车显示当前价与变价提示；旧预览提交返回 PRICE_CHANGED，不静默按新价建单、不删车；重新确认后的订单为 13880 分。下架/缺货项保留并标明原因，存在任一无效项即阻断整车结算，其他项不被部分结算；移除无效项后才可继续。 |
| 证据 | 前后商品、购物车与预览截图，旧/新预览响应、订单数与车项变化。 |

### T-C-05 可信金额、分币精度与最大整单金额

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-14、BR-01、§3.4.2；原 TC-15；契约 §2.1、§6.1、§9.2 |
| 优先级 | 高 |
| 方式/依赖 | 接口脚本＋数据库精确值核对；依赖 B 价格投影。 |
| 前置条件与具体数据 | 场景 A：P1 2990 分×3，P2 5900 分×1。场景 B：20 个可售商品单价均 99999999 分、库存均≥99，每行 99 件。 |
| 操作步骤 | 1. A 正常预览，提交 clientTotalFen=100。<br>2. 核对订单金额与行快照，并尝试附加自行计算的小计/优惠字段。<br>3. 独立数据执行 B 的预览和下单，核对接口、存储和页面金额。 |
| 预期结果 | A 仍为 14870 分即 148.70 CNY；运费、额外税费、优惠均为 0。clientTotalFen 是合法展示提示，不能因它被篡改就错误拒绝真实未变价订单；未知字段按建议契约拒绝。B 总额精确为 197999998020 分即 1979999980.20 CNY，无 32 位溢出、截断或浮点误差。创建待付款订单不扣/预占库存。 |
| 证据 | 篡改请求、各行与总金额精确值、最大值页面截图及数据库读取值。 |

### T-C-06 收货表单边界、必填和操作可达性

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-14、§3.4.2、NFR-03；原 TC-15、TC-38；契约 §2.1、§5.4 |
| 优先级 | 中 |
| 方式/依赖 | 参数化接口脚本＋手工键盘/三屏宽；依赖 E 表单壳、A 公共校验。 |
| 前置条件与具体数据 | 每组用 U1 有效单商品购物车和新预览；ADDR 为本篇有效地址；测试字符串按 Unicode 码点生成。 |
| 操作步骤 | 1. 逐字段替换：收件人长度 1/2/50/51，地址 4/5/200/201，电话规范化后 5/6/20/21 位；国家/城市若为自由文本测试 1/2/100/101 字符，若为受控选项测试有效值/非法值；必填字段为空或全空格。<br>2. 检查中国省市缺项及无省/州层级地区按共同配置的可空分支；备注省略/null/2000/2001 字符。<br>3. 在 390/768/1440 px 用键盘完成结算并触发一项错误。 |
| 预期结果 | 合法边界允许，越界/缺必填拒绝且订单不新增、购物车不丢失；错误定位具体字段，其他非敏感输入保留。备注上限 2000 与码点计数属于建议契约细节。页面不整体横向溢出，标签/焦点可识别，错误有文本，主要按钮可操作。 |
| 证据 | 字段样例表、响应与无写入核对；三宽度截图、键盘焦点/错误定位记录。 |

### T-C-07 预览的用户绑定、有效期及购物车版本

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-14、BR-06；原 TC-14、TC-16；契约 §6.1、§6.3 |
| 优先级 | 高 |
| 方式/依赖 | 接口脚本；依赖可控时钟、A 身份；15 分钟为待评审设计参数。 |
| 前置条件与具体数据 | U1/P1×1，时钟 2026-09-06T01:00:00Z 生成预览 PV1；U2 有相同商品的独立车。 |
| 操作步骤 | 1. U2 使用 PV1 提交；U1 使用伪造/篡改 PV1 提交。<br>2. U1 改数量到 2 后用 PV1 和旧 cartVersion 提交。<br>3. 独立复位，预览后推进到第 16 分钟再首次提交；随后重新预览、确认并提交。 |
| 预期结果 | 预览不能跨用户或篡改使用；旧购物车版本/内容拒绝并提示重新预览，不能丢失更新后的数量。按当前建议参数第 16 分钟首次执行时过期拒绝，重新预览可建单。所有失败不建单、不删车；已成功订单的同键重试不应重新检查预览，见 T-C-08。 |
| 证据 | 时钟、令牌所属用户、版本、响应、前后车项/订单数；避免在日志暴露完整会话凭据。 |

### T-C-08 创建订单的重复点击、响应丢失与 24 小时去重

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-14、BR-06；原 TC-16、TC-41；契约 §6.1—6.2、§7.2 |
| 优先级 | 高 |
| 方式/依赖 | 并发接口脚本＋响应丢弃代理；依赖 A 幂等、真实数据库、可控时钟。 |
| 前置条件与具体数据 | U1 购物车 P1×3/P2×1；同一有效预览与完整请求体 R，键 K1。 |
| 操作步骤 | 1. 两连接同屏障发送完全相同的 K1/R，丢弃其中一次服务端成功响应。<br>2. 购物车已空时再次发送 K1/R。<br>3. 把时钟推进首次成功后 23:59:59，确保原预览已过期，再发送 K1/R 并 GET 订单；检查保留配置/过期时间不少于成功时间加 24 小时。 |
| 预期结果 | 只有一个订单号/订单实体、一次车项移除、一次初始状态历史。并发完成后返回同一结果；短等待超限可 409 REQUEST_IN_PROGRESS，原键后续重试取回同一订单。完成重放按契约 200 与 Idempotency-Replayed:true；23:59:59 重试仍去重，不因空车/过期预览再次执行创建；保留期限至少 24 小时。恰满 24 小时及之后按团队确认的过期边界判断，不凭本例额外要求无限期重放。 |
| 证据 | 屏障与丢包配置、各响应/请求键摘要、23:59:59 时钟及保留配置/过期时间、订单/车版本/历史/成功审计计数。 |

### T-C-09 订单请求键冲突、资源绑定与重放前授权

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-14—15、BR-06、NFR-02；原 TC-16、TC-19、TC-32；契约 §6.2 |
| 优先级 | 高 |
| 方式/依赖 | 接口脚本；依赖 A 停用与会话能力。 |
| 前置条件与具体数据 | U1 已以 K1 创建 O1，保留原体 R；U1 另有 O2；U2 独立有效车。 |
| 操作步骤 | 1. U1 用 K1 改地址或备注提交创建，或仅更改 cartVersion。<br>2. U1 对 O1 付款成功使用 K2，再把同一付款操作的 K2 用于 O2。<br>3. U2 使用与 U1 相同 K1 提交自己的合法创建。<br>4. 停用 U1 后使用原会话/K1/R 重试。 |
| 预期结果 | 同用户同操作同键内容变化返回 IDEMPOTENCY_CONFLICT；目标订单 ID 也是摘要一部分，K2 不得把 O1 结果重放给 O2。不同用户键空间独立，U2 可生成自己的订单。停用后先拒绝授权，不借旧键读 O1；O1/O2 无额外扣库存、错误关联或重复记录。 |
| 证据 | 冲突响应、actor/operation/resource 绑定检查、两用户订单归属、停用后响应及库存对照。 |

### T-C-10 创建订单整体回滚

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-14、BR-06、NFR-01；原 TC-17；契约 §6.1、§7.1—7.2 |
| 优先级 | 高 |
| 方式/依赖 | 故障注入集成脚本；依赖 A UnitOfWork、F Audit、真实数据库。 |
| 前置条件与具体数据 | U1 有 P1×3/P2×1 及有效预览；保存车版本、各表逻辑计数与库存。 |
| 操作步骤 | 1. 每轮独立复位，分别在订单头写后、明细写入中、车项移除后、Audit.append 写入时注入异常。<br>2. 执行创建并读回所有关联数据。<br>3. 移除故障，用相同请求体/K 重试。 |
| 预期结果 | 每个失败点都不留下订单头/部分明细/地址、初始状态、成功幂等或成功审计；购物车项及版本回到原值、库存不变。移除故障后同键可创建完整唯一订单；不能因先写了幂等成功而返回不存在资源。客户端未知结果处理按 T-C-26。 |
| 证据 | 准确注入点和异常标识、事务前后逻辑快照、无孤立记录检查、同键恢复结果。 |

### T-C-11 模拟付款失败后成功的正常流程

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-15、BR-02—03；原 TC-18；契约 §5.4、§6.3、§7.2 |
| 优先级 | 高 |
| 方式/依赖 | 接口脚本＋页面；依赖 B 库存、F 审计。 |
| 前置条件与具体数据 | O1 为 U1 待付款订单，P1×3/P2×1，总额 14870 分；当前库存 P1=100/P2=100。 |
| 操作步骤 | 1. 页面选择模拟失败，随后读取订单/库存。<br>2. 获取当前版本，以新 K 选择模拟成功，再读详情与后台列表。 |
| 预期结果 | 失败有失败结果记录，订单仍 PENDING_PAYMENT、库存 100/100，可重试。成功金额按快照 14870 分，状态 PAID、库存 97/99、仅一次成功付款/扣减。前后台一致显示 SIMULATED 和“不发生真实扣款”，不要求银行卡/支付账户，不调用真实支付。 |
| 证据 | 两次请求、失败/成功业务记录、库存流水、前后台模拟标识与状态截图。 |

### T-C-12 商品变更、长期待付与历史快照保留

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-15—16、BR-01、BR-07、NFR-01；原 TC-21、TC-40；契约 §6.1、§9.3 |
| 优先级 | 高 |
| 方式/依赖 | 接口脚本＋可控时钟/服务重启；依赖 B 商品维护。 |
| 前置条件与具体数据 | U1 创建 O1：P1“测试商品一”×3、2990 分，ADDR；订单未付款。 |
| 操作步骤 | 1. 管理员把 P1 改名“新版测试商品一”、改价为 3990 分；U1 修改个人联系资料。<br>2. 推进测试时钟 48 小时，刷新/重登/重启后端读取 O1，再模拟成功付款。<br>3. 商品下架后再次读取已付 O1。 |
| 预期结果 | O1 在未主动取消时仍待付款，无自动过期；名称、SKU、数量、2990 分单价、8970 分金额和地址联系方式快照不变。48 小时后商品仍可售且库存足够可按旧快照付款；下架不删除/改写历史。付款时当前可售/库存仍检查，单独由 T-C-13 验证。 |
| 证据 | 变更前后商品/个人资料与订单快照、时钟/重启记录、付款金额和历史数据对照。 |

### T-C-13 付款时重新校验可售与多商品库存

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-15、BR-02、BR-06；原 TC-17、TC-21；契约 §7.1—7.3 |
| 优先级 | 高 |
| 方式/依赖 | 参数化集成脚本；依赖 B 实际库存服务。 |
| 前置条件与具体数据 | U1 待付 O1 含 P1×3/P2×2；建单时库存足够。 |
| 操作步骤 | 1. 分轮执行：将 P2 库存改为 1，或将 P2 下架，保持 P1 库存 100。<br>2. 请求模拟成功，核对订单、付款、两商品库存/流水。<br>3. 恢复 P2 上架且库存 2，获取当前状态/版本，用重新确认的新 K 付款。 |
| 预期结果 | 前两轮均拒绝成功付款，指出不足/不可售商品；O1 留待付款，P1 不被部分扣减，P2 不变，无成功付款或扣减流水。恢复条件后全单一次扣减 P1=97/P2=0；使用价格快照，不重新按现价计价。 |
| 证据 | 各商品初值与状态、逐项错误、全单流水/订单/付款记录、恢复后的扣减结果。 |

### T-C-14 同一订单并发付款、重放与去重期后保护

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-15、BR-02—03、BR-06；原 TC-19；契约 §6.2—6.3、§9.2 |
| 优先级 | 高 |
| 方式/依赖 | 并发接口脚本；依赖 B、A 幂等及可控时钟。 |
| 前置条件与具体数据 | O1 待付，P1×3，库存 10。独立子例使用同键同体，及不同键同 expectedVersion。 |
| 操作步骤 | 1. 各子例用两个连接同时提交模拟成功。<br>2. 成功后同键同体重放，再以新键/当前版本分别提交 SUCCESS 和 FAILURE。<br>3. 仅在测试环境让去重保留期结束，继续用新键尝试付款。 |
| 预期结果 | 最多一次成功付款业务效果，库存 7、唯一扣减流水、状态 PAID。相同键重放原结果；不同键競争可版本/状态冲突。已付后新键按建议契约 STATE_CONFLICT，不能倒退或记录第二次成功；去重记录到期也不能再次扣款/扣库存。 |
| 证据 | 并发时序、键摘要/版本、响应、成功记录/库存流水计数、过期后状态与库存。 |

### T-C-15 最后一件库存与后台调整竞争

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-15、FR-27、BR-02、BR-06；原 TC-20、TC-34；契约 §7.1—7.3、§9.2 |
| 优先级 | 高 |
| 方式/依赖 | 真实数据库并发脚本；C+B 协作，B 提供库存调整入口。 |
| 前置条件与具体数据 | A 轮：库存 P1=1，U1/U2 各有 P1×1 待付订单。B 轮：库存 P1=1，U1 有 P1×1 待付订单，管理员准备增加 2。 |
| 操作步骤 | 1. A 轮两个用户同屏障模拟成功付款。<br>2. B 轮将付款与后台增加 2 同屏障发出；新一轮把增加改为减少 1。<br>3. 每轮读订单、库存余额/全流水，并用初值+调整+返还−成功扣减复算。 |
| 预期结果 | A 至多一单 PAID；在无其他故障的受控条件下恰有一单成功，另一单仍待付并获缺货/可重试冲突，库存 0。B 增加 2 与付款均成功后库存为 2，无丢失更新；减少 1 与付款竞争只能按合法顺序至多一个扣减成功，余额 0、库存不负。 |
| 证据 | 同步发起记录、各响应及提交顺序、逐商品余额恒等式、另一单待付状态。 |

### T-C-16 付款写入或审计故障的事务回滚

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-15、BR-02、BR-06、NFR-01；原 TC-17；契约 §7.1—7.2 |
| 优先级 | 高 |
| 方式/依赖 | 故障注入集成脚本；依赖 B 库存、A UnitOfWork、F Audit。 |
| 前置条件与具体数据 | O1 待付，P1×3/P2×1；库存 10/10，记录订单版本和历史。 |
| 操作步骤 | 1. 分轮在第一件商品扣减后、付款记录写入后、订单状态更新后、成功审计写入时抛错。<br>2. 请求模拟成功后读订单、库存、流水、付款与幂等。<br>3. 关闭故障，以原 K/请求体重试。 |
| 预期结果 | 失败后订单恢复原待付版本/历史；库存仍 10/10，无部分扣减、成功付款、成功幂等或成功审计。移除故障后的重试成功且仅扣到 7/9；库存服务不得另开事务提前提交。 |
| 证据 | 故障位置、全事务前后快照、重试响应、流水与成功事件计数。 |

### T-C-17 待付款取消、原因校验与取消后禁止付款

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-17、BR-02—03、§3.4.2；原 TC-22；契约 §5.4、§6.3 |
| 优先级 | 高 |
| 方式/依赖 | 参数化接口脚本＋取消确认页面；依赖 A 身份。 |
| 前置条件与具体数据 | U1 待付 O1=P1×2，库存 10；用独立订单分别验证本人和管理员取消。 |
| 操作步骤 | 1. 尝试空白、1 字符、501 字符原因；再使用 2/500 字符合规原因。<br>2. 页面取消前确认，合法取消后重复同键请求、新键取消，再尝试付款。 |
| 预期结果 | 不合法原因拒绝且订单不变；合规取消记操作人、原因和时间，转 CANCELLED，库存仍 10，无退款/返还记录。重复取消不追加历史或库存效果；再次付款拒绝，不能恢复已取消订单。本人和管理员遵守相同状态约束。 |
| 证据 | 原因边界响应、确认页面、状态历史、付款/退款/库存计数。 |

### T-C-18 已付款取消、下架商品返还与一次退款

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-17、BR-02—03、BR-07；原 TC-22；契约 §6.2—6.3、§7.1、§9.2 |
| 优先级 | 高 |
| 方式/依赖 | 并发接口脚本＋页面；依赖 B 按原扣减返还。 |
| 前置条件与具体数据 | O1 已付 P1×3/P2×1，付款前库存 10/10、现为 7/9；先将 P2 下架。 |
| 操作步骤 | 1. 本人和管理员分别以不同 K、相同当前版本同时取消 O1。<br>2. 成功后同键重放，再新键重复取消；读全部库存与退款/历史。<br>3. 尝试再次发货或付款。 |
| 预期结果 | 恰一次有效整单取消与“模拟退款完成”，退款金额等于原已付金额；库存恢复 10/10，即使 P2 下架也按原扣减恢复。竞争失败/重放不额外加库存或退款。订单 CANCELLED，不能再付/发，不触发真实退款。 |
| 证据 | 并发响应、下架状态、原扣减与对应返还流水、一笔模拟退款及唯一取消历史、界面标识。 |

### T-C-19 已付款取消中途失败的回滚

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-17、BR-06、NFR-01；原 TC-17、TC-22；契约 §7.2 |
| 优先级 | 高 |
| 方式/依赖 | 故障注入集成脚本；依赖 A、B、F。 |
| 前置条件与具体数据 | O1 已付 P1×3/P2×1，库存 7/9，初始状态 PAID。 |
| 操作步骤 | 1. 分轮在首商品返还后、退款记录写入后、订单取消后、审计写入时注入异常。<br>2. 请求取消并读回订单、退款、库存和历史。<br>3. 移除故障后同键重试。 |
| 预期结果 | 失败轮均保持 PAID、库存 7/9、原版本/历史，无退款成功/部分返还/取消成功幂等；同键恢复后整体取消、恢复到 10/10且一次模拟退款。若前端未收到明确业务拒绝，按未知结果核查，不能误导“确定失败”。 |
| 证据 | 各失败位置、跨域前后快照、同键恢复和唯一返还/退款证据。 |

### T-C-20 同一待发货订单的取消与发货竞争

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-17—18、BR-02—03、BR-06；原 TC-23；契约 §6.3、§7.3 |
| 优先级 | 高 |
| 方式/依赖 | 并发脚本；依赖 A、B，两个独立会话。 |
| 前置条件与具体数据 | O1=PAID、P1×3，付款前库存 10、当前 7；本人取消原因“测试取消”，ADMIN 发货“测试物流”/“TEST-C-001”；双方持相同 version。 |
| 操作步骤 | 1. 同屏障提交本人取消和管理员发货。<br>2. 刷新前后台，读取订单、退款、运单、库存和全部历史。<br>3. 失败方不自动换版本重试，先检查当前允许动作。 |
| 预期结果 | 只有一种合法结果：取消成功则 CANCELLED、库存 10、一笔退款、无成功运单；发货成功则 SHIPPED、库存 7、有模拟运单、无退款/返还。不得两者都成功或出现状态与库存不一致；失败方获版本/状态冲突及可读当前状态。 |
| 证据 | 屏障时间线、两个响应、终态/库存/运单/退款/历史一致性表、刷新截图。 |

### T-C-21 付款与取消竞争及合法顺序

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-15、FR-17、BR-02—03、BR-06；原 TC-19、TC-22、TC-23；契约 §6.3、§7.3 |
| 优先级 | 高 |
| 方式/依赖 | 并发脚本；依赖 B 库存和订单版本检查。 |
| 前置条件与具体数据 | O1 待付、P1×3、库存 10；U1 两会话分别准备成功付款与取消，持相同版本。 |
| 操作步骤 | 1. 同屏障发送付款/取消。<br>2. 核对响应和最终状态。<br>3. 若付款胜出而取消版本冲突，用户刷新并再次确认取消，用新键/新版本提交。 |
| 预期结果 | 取消先成功则 CANCELLED、库存 10、无成功付款；付款先成功则 PAID、库存 7，旧版本取消冲突。随后重新确认的合法取消可成功，最终 CANCELLED、库存 10、一次付款和一次退款。这不应误判为“付款与取消永远不可先后成功”；任何调度无双扣/双返。 |
| 证据 | 并发响应/版本、重新确认记录、各阶段库存与付款/退款计数。 |

### T-C-22 整单模拟发货、本人收货与重复确认

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-18、BR-02—03、§3.4.2；原 TC-24；契约 §5.4、§6.3 |
| 优先级 | 中 |
| 方式/依赖 | 接口脚本＋前后台页面；依赖 A、F 审计。 |
| 前置条件与具体数据 | O1=PAID，P1×3/P2×1；当前库存 7/9；管理员准备“测试物流”/“TEST-C-002”。 |
| 操作步骤 | 1. 后台筛选待发货并核对地址，填写运单、确认整单发货。<br>2. U1 刷新详情，二次确认收货；同键重放，并新键重复确认。<br>3. 核对后台与本人页面，以及两商品库存。 |
| 预期结果 | 发货后 SHIPPED，有操作人、时间、模拟物流/运单号；本人收货后 COMPLETED。全过程库存维持 7/9，无第二次扣减或退款。重复收货不新增完成历史/业务效果；前后台号、模拟标识、状态一致，无真实物流调用或部分发货。 |
| 证据 | 操作截图、状态历史/版本、模拟运单字段、重复请求和库存对照。 |

### T-C-23 非法发货/收货、字段边界及订单快照不可改

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-16—18、BR-01、BR-03、§3.4.2；原 TC-21、TC-24；契约 §2.1、§5.4、§6.3 |
| 优先级 | 高 |
| 方式/依赖 | 状态×角色参数化脚本；依赖 A。 |
| 前置条件与具体数据 | 通过业务流程构造 U1 的 PENDING_PAYMENT/PAID/SHIPPED/COMPLETED/CANCELLED 五种订单。 |
| 操作步骤 | 1. 对非 PAID 状态发货，对非 SHIPPED 状态收货；U2/ADMIN 尝试代本人收货，普通用户尝试发货。<br>2. 有效 PAID 单测试物流名 1/2/50/51 字符，运单号 2/3/50/51、下划线或空格。<br>3. 管理命令附加商品、数量、地址、金额、status、部分发货 items。 |
| 预期结果 | 只允许管理员发货 PAID、本人才可收货 SHIPPED；其他组合拒绝。物流名 2—50、运单 3—50 字母/数字/连字符合法，非法值不发货。未定义的快照/状态/部分发货字段按契约拒绝，所有原始成交快照、状态与库存不变。 |
| 证据 | 角色状态矩阵、边界样例/错误字段、失败前后订单/快照/库存/历史。 |

### T-C-24 本人订单分页、排序、筛选与详情入口

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-16、FR-11、§3.4.2；原 TC-11、TC-21；契约 §2.2、§5.4 |
| 优先级 | 中 |
| 方式/依赖 | 接口脚本＋页面；依赖 A，F 提供关联咨询入口。 |
| 前置条件与具体数据 | U1 准备 6 单：待付 2、已付/已发/完成/取消各 1，创建时间不同；U2 1 单；第三用户无订单。 |
| 操作步骤 | 1. U1 按 pageSize=2 连续读 3 页，分别按五个状态筛选。<br>2. 输入 page=0、pageSize=51、非法状态。<br>3. 查看每状态详情的快照/历史/允许动作及咨询入口；管理员改变状态后重新请求。<br>4. 空账户访问订单列表。 |
| 预期结果 | U1 结果只含本人 6 单，时间倒序、翻页不重不漏、筛选集合精确；非法参数拒绝。详情显示订单号/时间/商品与地址快照/金额/付款/历史；按钮符合状态，服务端仍校验。刷新可见后台变化；空态有继续浏览入口，咨询关联的是当前本人订单。 |
| 证据 | 预期订单 ID/时间集合、分页与筛选断言、各状态详情与空态截图。 |

### T-C-25 购物车并发修改与结算不能丢失新商品

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-13—14、BR-06；原 TC-14、TC-16；契约 §6.1、§6.3、§7.3 |
| 优先级 | 高 |
| 方式/依赖 | 并发接口脚本；依赖 B、真实数据库。 |
| 前置条件与具体数据 | U1 车仅 P1×1，预览 PV1/cartVersion=v；另一会话准备加 P2×1；P1/P2 库存充足。 |
| 操作步骤 | 1. 两连接同时提交 PV1 建单和新增 P2。<br>2. 分轮另测两个会话用同一 cartVersion 把 P1 分别设置为 2/3。<br>3. 读取订单快照、当前购物车及版本。 |
| 预期结果 | 新增 P2 先提交则旧预览建单应 CART_CHANGED，不删车；建单先提交则订单只含 P1，后加的 P2 留在车内。不得创建未确认的含 P2 订单或把新 P2 清掉。两个改量仅一个有效，另一个 VERSION_CONFLICT；不存在静默覆盖。所有结果库存不变。 |
| 证据 | 并发时序与响应、订单商品集合/车项集合及版本，验证与合法串行次序一致。 |

### T-C-26 超时、服务异常及未知结果恢复

| 字段 | 内容 |
| --- | --- |
| 关联依据 | SRS FR-14—18、BR-06、NFR-03、§3.1.3；原 TC-41；契约 §2.3、§6.2 |
| 优先级 | 高 |
| 方式/依赖 | 故障代理＋页面＋接口脚本；依赖 E 状态组件、A 请求设施。 |
| 前置条件与具体数据 | U1 有待付 O1和有效结算车；准备不存在订单 ID，以及返回 500 的受控测试入口。 |
| 操作步骤 | 1. 分轮模拟创建/付款已提交但响应丢失，以及请求未到达服务器的超时。<br>2. 查看页面，再通过列表/详情查询或用原 K/原体重试。<br>3. 分别触发明确字段错误、403、404、500 与空列表。 |
| 预期结果 | 两类超时及 500 均不能直接断言业务失败，提示结果暂未确认并提供查询/原请求重试；已成功只得到原资源/效果，未到达请求可安全执行一次。已确认 4xx、无权限、不存在、空列表、异常分别反馈，500 不泄露 SQL/堆栈/路径；输入和重试键不被错误替换。 |
| 证据 | 故障生效点、响应是否到达、各状态截图、查询/原键恢复记录、订单与库存唯一效果。 |

## 4. 需求与原场景覆盖

| 依据 | 本篇细化用例 |
| --- | --- |
| FR-13；原 TC-13、TC-14 | T-C-01—04、07、25 |
| FR-14；原 TC-14—17 | T-C-03—10、25—26 |
| FR-15；原 TC-17—20 | T-C-03、09、11—16、21、26 |
| FR-16；原 TC-11、TC-21 | T-C-03、12—13、23—24 |
| FR-17；原 TC-22—23 | T-C-17—21、23、26 |
| FR-18；原 TC-23—24 | T-C-03、20、22—23、26 |
| BR-01（金钱与快照） | T-C-04—05、12、23 |
| BR-02（库存） | T-C-01—02、05、11、13—22 |
| BR-03（状态机） | T-C-11、14、17—23 |
| BR-06（幂等/并发/回滚） | T-C-07—10、13—21、25—26 |
| BR-07、NFR-01—02 | T-C-01、03、09—10、12、16、18—19、23 |
| NFR-03 的本模块配合；原 TC-38、TC-41 | T-C-06、26；全站浏览器矩阵由 E/F 汇总 |
| FR-27/原 TC-34 的协作覆盖 | T-C-15；B 仍负责库存后台自身的完整测试 |

## 5. 待确认事项与执行衔接

1. 确认本稿案例粒度、风险顺序以及契约建议能否作为实施检查细则；业务规则中的 1—99 件、20 行、零运费、24 小时最低去重、无待付自动过期、付款时扣库存、整单退款与状态机均已由 SRS 明确，不列为待定。
2. 首次设计评审确认路径、字段、错误码、15 分钟预览有效期、CSRF/版本机制及未定义字段统一拒绝等设计参数。若改动，保持业务判据不变并更新操作步骤。
3. A/B/C/F 需提供真实数据库测试环境、可控时钟、屏障并发与事务故障注入方式；后续把本稿步骤转为可执行脚本并补齐实际版本/结果/证据。故障点和重试策略须可在独立测试环境启停。
4. 共同地区配置决定哪些地区没有省/州层级，执行 T-C-06 前选定一条可复用测试记录；已规定的条件必填规则不更改。
5. C 配合 F 执行原 TC-39 本人订单读取/购物车页面性能及最终集成验收；本篇不以尚无环境的设计稿替代性能测量，也不代替 E 的全站两浏览器检查。

**当前结论：26 个用例已设计，等待用户审核；全部未执行，无通过或失败结论。**
